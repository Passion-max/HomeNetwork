{
  "name": "homenetwork",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Premium management dashboard for the ZTE F670L GPON ONT",
  "scripts": {
    "probe": "node --env-file=.env src/probe.mjs",
    "poll": "node --experimental-sqlite --env-file=.env src/poller.mjs",
    "start": "node --experimental-sqlite --env-file=.env src/server.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
